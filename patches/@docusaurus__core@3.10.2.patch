diff --git a/lib/server/codegen/codegen.js b/lib/server/codegen/codegen.js
index 1a622545264f518381ccfc07c6f7e45f12ce09d2..6751cdca615f5fd702ac0accbd71415d2ca4858c 100644
--- a/lib/server/codegen/codegen.js
+++ b/lib/server/codegen/codegen.js
@@ -54,6 +54,16 @@ function genSiteStorage({ generatedFilesDir, siteStorage, }) {
     return (0, utils_1.generate)(generatedFilesDir, 'site-storage.json', JSON.stringify(siteStorage, null, 2));
 }
 async function generateSiteFiles(params) {
+    // Write {"type":"commonjs"} so webpack treats .docusaurus/ files as CJS
+    // even when the host project has "type":"module" in its package.json.
+    // client-modules.js uses require() inside an ESM export — without this,
+    // webpack leaves require() as-is (undefined in the browser).
+    const fs_1 = require("fs-extra");
+    const path_1 = require("path");
+    // No "type" field → webpack uses javascript/auto for .js files in this dir,
+    // which allows the mixed ESM/CJS syntax Docusaurus generates (export default
+    // combined with require() calls in client-modules.js and routes files).
+    await fs_1.outputFile(path_1.join(params.generatedFilesDir, 'package.json'), '{}');
     await Promise.all([
         genWarning(params),
         genClientModules(params),
diff --git a/lib/ssg/ssgNodeRequire.js b/lib/ssg/ssgNodeRequire.js
index cfd85dbde0b9a52996b7321bef85eafb7b29ec17..997a5ede76a8035f01d15838a06c7b28315e306a 100644
--- a/lib/ssg/ssgNodeRequire.js
+++ b/lib/ssg/ssgNodeRequire.js
@@ -23,9 +23,25 @@ function createSSGRequire(serverBundlePath) {
     const realRequire = (0, module_1.createRequire)(serverBundlePath);
     const allRequiredIds = [];
     const ssgRequireFunction = (id) => {
-        const module = realRequire(id);
+        let mod;
+        try {
+            mod = realRequire(id);
+        }
+        catch (err) {
+            const code = err.code;
+            const msg = String(err.message);
+            // Externalized ESM theme files import @theme/* webpack aliases (not real
+            // packages) and .css files (not valid ESM) — neither resolves in Node.js
+            if (
+                (code === 'ERR_MODULE_NOT_FOUND' && msg.includes('@theme/')) ||
+                (code === 'ERR_UNKNOWN_FILE_EXTENSION' && msg.includes('.css'))
+            ) {
+                return {};
+            }
+            throw err;
+        }
         allRequiredIds.push(id);
-        return module;
+        return mod;
     };
     const cleanup = () => {
         allRequiredIds.forEach((id) => {
@@ -36,5 +52,10 @@ function createSSGRequire(serverBundlePath) {
     ssgRequireFunction.cache = realRequire.cache;
     ssgRequireFunction.extensions = realRequire.extensions;
     ssgRequireFunction.main = realRequire.main;
+    // webpack's require.resolveWeak() is a compile-time transform on the client
+    // but the server bundle eval context doesn't define it — polyfill as no-op
+    ssgRequireFunction.resolveWeak = () => undefined;
+    // CSS files required by the server bundle have no meaningful value at SSR time
+    ssgRequireFunction.extensions['.css'] = (mod) => { mod.exports = {}; };
     return { require: ssgRequireFunction, cleanup };
 }
diff --git a/lib/ssg/ssgRenderer.js b/lib/ssg/ssgRenderer.js
index 5f208254372515a3b301ead6948d3f40996ed582..ebb2ff48b875ed811b6e197964018aa49f1a3271 100644
--- a/lib/ssg/ssgRenderer.js
+++ b/lib/ssg/ssgRenderer.js
@@ -21,6 +21,10 @@ const ssgEnv_1 = require("./ssgEnv");
 const ssgUtils_1 = require("./ssgUtils");
 const ssgNodeRequire_1 = require("./ssgNodeRequire");
 async function loadAppRenderer({ serverBundlePath, }) {
+    // webpack emits CJS chunks but the project "type":"module" makes Node.js
+    // treat them as ESM.  A package.json override in the server dir fixes this.
+    const serverDir = path_1.default.dirname(serverBundlePath);
+    await fs_extra_1.default.outputFile(path_1.default.join(serverDir, 'package.json'), '{"type":"commonjs"}');
     const source = await logger_1.PerfLogger.async(`Load server bundle`, () => fs_extra_1.default.readFile(serverBundlePath));
     const filename = path_1.default.basename(serverBundlePath);
     const ssgRequire = (0, ssgNodeRequire_1.createSSGRequire)(serverBundlePath);
